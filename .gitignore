# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*
!.env.example

# vercel
.vercel

# claude / impeccable / playwright working state
.claude/settings.local.json
.impeccable/live/
.impeccable/config.local.json
.playwright-cli/

# other project, not part of DOCTORY
Client-built.zip

# Prisma generated client (regenerated by postinstall)
/src/generated/

# SQLite demo database (recreated by `npm run db:reset`)
/prisma/*.db
/prisma/*.db-journal

# Review screenshots dropped in the repo root by browser tooling.
# Real image assets live in public/, so root-level PNGs are always transient.
/*.png

# Scratch capture scripts used during review.
.*.mjs
